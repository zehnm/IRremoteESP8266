# The tools/ directory is a flat set of host-side CLIs that share local headers
# (e.g. uccode_common.h). cpplint's root=src makes it flag those sibling
# includes as needing a subdirectory, which does not apply here.
filter=-build/include_subdir
